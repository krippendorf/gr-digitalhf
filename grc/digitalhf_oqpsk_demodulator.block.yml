# auto-generated by grc.converter

id: digitalhf_oqpsk_demodulator
label: oqpsk_demodulator
category: '[digitalhf]'

parameters:
-   id: fs
    label: fs (Hz)
    dtype: float
-   id: fd
    label: fd (Hz)
    dtype: float
-   id: loop_bw
    label: loop_bw
    dtype: float
-   id: df
    label: df (Hz)
    dtype: float

inputs:
-   domain: stream
    dtype: complex

outputs:
-   label: soft_dec_x
    domain: stream
    dtype: float
-   label: soft_dec_y
    domain: stream
    dtype: float

templates:
    imports: import digitalhf
    make: digitalhf.oqpsk_demodulator(${fs}, ${fd}, ${loop_bw}, ${df})

file_format: 1
